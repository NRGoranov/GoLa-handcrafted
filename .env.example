SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
TO_EMAIL=

# Or use the same variables as DiplomnaWork (recommended if already working there):
EMAIL_USER=
EMAIL_PASSWORD=

# Admin panel (/admin)
ADMIN_PASSWORD=
ADMIN_SESSION_SECRET=

# Supabase (recommended on Vercel for persistent sections + inquiry inbox)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
